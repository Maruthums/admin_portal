import React from "react";
import { Box, Typography } from "@mui/material";

export const PhotoList = () =>{

    return(
        <Box>
            <Typography>
                Photo
            </Typography>
        </Box>
    )
}